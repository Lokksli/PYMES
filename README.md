# PYMES

Flask based messenger

Run locally:

1. Create a venv and install requirements:

   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt

2. Run the app:

   python messenger.py

3. Open http://localhost:5000/ 

# PYMES
